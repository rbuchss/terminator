#!/bin/bash
# shellcheck source=/dev/null
source "${HOME}/.tmux/src/command.sh"

# This is a wrapper for tmuxinator to call. Eg:
# tmux_command: ~/.homesick/repos/terminator/tmux/bin/tmux-command-invoke
tmux::command::invoke "$@"
